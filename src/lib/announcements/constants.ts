// Shared announcement limits are exported separately so both server validators and client forms stay consistent.
export const ANNOUNCEMENT_TITLE_MAX = 120;
export const ANNOUNCEMENT_BODY_MAX = 2000;
