/**
 * @state.route /
 * @state.area public
 * @state.capabilities view:list
 * @state.notes Auto-seeded capability annotation for snapshot v2; refine when workflows change.
 */
export default function Home() {
  return <div>EduHub – App is running</div>;
}
